/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep the old static landing page working during transition
  // The api/waitlist.js will be superseded by src/app/api/waitlist/route.ts
}

module.exports = nextConfig
